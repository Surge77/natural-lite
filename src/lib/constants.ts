/** Scroll distance past which the header pins and condenses. */
export const STICKY_HEADER_OFFSET = 120;

/** localStorage key for the cart. Bump the suffix on any shape change. */
export const CART_STORAGE_KEY = 'natural-lite:cart:v1';

/** Mirrors --breakpoint-md in tokens.css: the width at which the footer link
 *  columns stop being accordions and stand open side by side. */
export const MD_BREAKPOINT_QUERY = '(min-width: 48rem)';

/** Shared by every reveal-on-scroll observer. */
export const REVEAL_THRESHOLD = 0.15;
export const REVEAL_ROOT_MARGIN = '0px 0px -10% 0px';
